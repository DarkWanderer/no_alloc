[package]
name = "no_alloc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Static allocation checker for Rust: proves annotated functions never reach the global allocator."

[dependencies]
no_alloc_macros = { workspace = true }
