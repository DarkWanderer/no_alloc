[package]
name = "no_alloc_report"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Stable-only report/verdict types, root-spec parsing, sidecar serde, path rendering for no_alloc."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
